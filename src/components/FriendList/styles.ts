import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 20
  },

  likes: {
    fontSize: 22,
    color: 'white',
    marginVertical: 10,
    textAlign: 'right',
  }
});